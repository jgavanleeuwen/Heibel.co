<!DOCTYPE html>
<html>
	<head>
		<%= include ../head.html.ejs %>
	</head>
	<body>

		<!-- Navigation -->
		<div class="navbar navbar-inverse navbar-static-top">
			<div class="container">
				<div class="navbar-header">
					<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</button>
					<a class="navbar-brand" href="#">Project name</a>
				</div>
				<div class="navbar-collapse collapse">
					<form class="navbar-form navbar-right">
						<% if (typeof user !== "undefined") { %>
							<span><strong>Welkom</strong>, <a href="account" data-bypass="true"><%= user.displayName %></a></span>
							<a href="logout" class="btn btn-danger" data-bypass="true">Sign out</a>
						<% } else {%>
							<a href="login" class="btn btn-success" data-bypass="true">Sign in</a>
						<% } %>
					</form>
				</div>
			</div>
		</div>

		<!-- Hero -->
		<section id="hero">
			<div class="jumbotron">
				<div class="container">
					<div class="col-md-7">
						<h1>Your account!</h1>
						<p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Account -->
		<section id="account">
			<div class="container">
				<div class="row">
					<div class="col-md-6">
						<h2 class="form-signin-heading">Name and account</h2>
						<div class="form-group">
							<label for="">Firstname</label>
						  <input type="text" class="form-control" disabled="true" name="firstname" placeholder="Enter firstname" required="true" value="<%= user.name.first %>">
						</div>
						<div class="form-group">
							<label for="">Lastname</label>
						  <input type="text" class="form-control" disabled="true" disabled="true" name="lastname" placeholder="Enter lastname" required="true" value="<%= user.name.last %>">
						</div>
						<div class="form-group">
							<label for="">Email address</label>
						  <input type="text" class="form-control" disabled="true" name="email" placeholder="Enter email-address" required="true" value="<%= user.email %>">
						</div>
						<p class="pull-right"><a href="account/edit" data-bypass="true" class="btn btn-success" role="button">Edit</a></p>
					</div>
					<div class="col-md-4 col-md-offset-2">
						<h2 class="form-signin-heading">About you</h2>
						<p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting.</p>
					</div>
				</div>
			</div>
		</section>
		<section id="footer">
			<%= include ../footer.html.ejs %>
		</section>
	</body>